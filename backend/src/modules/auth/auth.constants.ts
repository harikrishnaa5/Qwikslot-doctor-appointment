/** Access token JWT lifetime (matches @fastify/jwt sign options). */
export const ACCESS_TOKEN_EXPIRES_IN = "12h";

/** Seconds until access token expires (12 hours). */
export const ACCESS_TOKEN_EXPIRES_IN_SECONDS = 12 * 60 * 60;
