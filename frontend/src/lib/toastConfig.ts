/** Visible duration before exit animation starts (ms). */
export const TOAST_DURATION_MS = 3000;

/** Reveal from right edge (ms). */
export const TOAST_ENTER_MS = 480;

/** Collapse to right edge + fade (ms). */
export const TOAST_EXIT_MS = 620;

export const SIGN_IN_SUCCESS_MESSAGE = "Successfully signed in";
